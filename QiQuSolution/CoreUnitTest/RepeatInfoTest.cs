using System;
using Core;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace CoreUnitTest
{
    [TestClass]
    public class RepeatInfoTest
    {
        [TestMethod]
        public void TestRepeatInfo()
        {
            //RepeatInfo i = new RepeatInfo(TimeSpan.FromMinutes(1));
            //DateTime lastTime = i.NextTriggerTime;
            //i.WhenBeginDownload();
            //Assert.IsTrue(i.IsStarted);
            //Assert.IsFalse(i.IsDownloadSuccess);
            //Assert.AreEqual<DateTime>(lastTime, i.LastTriggerTime.Value);
            //i.WhenDownloaded();
            //i.WhenStopRepeat();
            
        }
    }
}
