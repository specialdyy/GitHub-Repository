using System;
using System.Collections.Generic;
using System.Linq;
using Core;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace CoreUnitTest
{
    [TestClass]
    public class SourceDataManagerTest
    {
        [TestMethod]
        public void TestSourceDataManager()
        {
            //SourceDataManager mgr = SourceDataManager.GetInstance();
            //List<SourceData> data = DataDownloader.DownloadData();
            ////刚下载获取到数据时：
            //mgr.AddSourceDatas(data, true);
            ////data.ForEach(d => { mgr.AddSourceDatas(d, true); });
            //SourceData d1 = mgr.GetTopNSourceData(1).SingleOrDefault();
            //Assert.AreEqual<SourceData>(data[0], d1);

            //mgr.SourceDatas.Clear();
            //mgr.StartRepeatDownload();
        }
    }
}
